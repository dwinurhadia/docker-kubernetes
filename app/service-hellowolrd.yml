apiVersion: v1
kind: Service
metadata:
  name: nodeexpress-service
spec:
  selector:
    app: nodeexpress-pod
  ports:
  - port: 31001
    nodePort: 31001
    targetPort: nodejs-port
    protocol: TCP
  type: NodePort
